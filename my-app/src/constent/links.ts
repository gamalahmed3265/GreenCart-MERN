export enum Pages {
  HOME = "/",
  CART = "/cart",
  PRODUCTS = "/products",
  PRODUCT = "/products/:id",
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  ORDERS = "/orders",
  DASHBOARD = "/dashboard",
}
